coordinates = (3, 7)
# this can no longer be changed

print(coordinates[1])

# Tuples are used less often than lists. But are useful for storing data that will never change.
# They are not to be confused with lists. Lists can have their data manipulated and changed.